#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
SRC="${ROOT}/public/template-starter"
OUT="${SRC}/starter.zip"

cd "${SRC}"
rm -f starter.zip
zip -r starter.zip manifest.json styles slots

echo "Built ${OUT}"
