# Name of your application. Used to uniquely configure containers.
service: workspace

# Name of the container image (use your-user/app-name on external registries).
image: workspace

# Deploy to these servers.
servers:
  web:
    - 192.168.0.1
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bundle exec sidekiq -C config/sidekiq.yml

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
#
# proxy:
#   ssl: true
#   host: app.example.com

# Where you keep your container images.
registry:
  # Alternatives: hub.docker.com / registry.digitalocean.com / ghcr.io / ...
  server: localhost:5555

  # Needed for authenticated registries.
  # username: your-user

  # Always use an access token rather than real password when possible.
  # password:
  #   - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - MCWEB_SMTP_USERNAME
    - MCWEB_SMTP_PASSWORD
    - MCWEB_S3_ACCESS_KEY_ID
    - MCWEB_S3_SECRET_ACCESS_KEY
  clear:
    # Required public-origin and reverse-proxy boundary. Replace every example.
    MCWEB_PUBLIC_URL: https://app.example.com
    MCWEB_ALLOWED_HOSTS: app.example.com
    MCWEB_TRUSTED_PROXIES: 172.16.0.0/12
    MCWEB_BIND: 0.0.0.0

    # Required SMTP delivery. Use `none` only for a trusted private relay.
    MCWEB_SMTP_ADDRESS: smtp.example.com
    MCWEB_SMTP_PORT: 587
    MCWEB_SMTP_AUTHENTICATION: plain
    MCWEB_SMTP_TLS: starttls
    MCWEB_MAIL_FROM: "McWeb <noreply@example.com>"

    # Required private S3-compatible object storage.
    MCWEB_ACTIVE_STORAGE_SERVICE: private_s3
    MCWEB_S3_BUCKET: replace-with-private-bucket
    MCWEB_S3_REGION: us-east-1
    # Leave blank for AWS S3; use an HTTPS URL for compatible providers.
    MCWEB_S3_ENDPOINT:
    MCWEB_S3_FORCE_PATH_STYLE: 0

    # Redis for Sidekiq (required for background jobs in production).
    # REDIS_URL: redis://redis:6379/0

    # Sidekiq worker concurrency (default: 5 in config/sidekiq.yml)
    # JOB_CONCURRENCY: 5

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Match this to any external database server to configure Active Record correctly
    # Use workspace-db for a db accessory server on same machine via local kamal docker network.
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Persistent application state that is not an upload store. Production
# Active Storage is required to use the private_s3 service above.
volumes:
  - "workspace_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: 4.0.6
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: valkey/valkey:8
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
