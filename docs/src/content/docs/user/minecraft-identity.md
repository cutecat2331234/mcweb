---
title: Minecraft 身份
description: 绑定 Minecraft 身份、选择主账号、刷新皮肤并安全解除不再使用的绑定。
edition: ce
audience:
  - user
sidebar:
  order: 2
---

从账户侧栏进入 Minecraft 页面。默认绑定流程是在游戏内执行站点显示的绑定命令，再把一次性验证码提交到网页。

## 绑定与主账号

1. 在受支持的服务器中执行页面给出的绑定命令。
2. 在有效期内把验证码填入网页。
3. 绑定成功后，可在多个身份之间选择主账号；受策略保护的变更可能进入等待或审核状态。

验证码过期、已使用或来自其他账户时不会建立绑定。不要向他人发送绑定验证码。

## 皮肤与缓存

页面会显示当前缓存皮肤及刷新状态。刷新是受限操作，短时间重复请求可能进入冷却。外部皮肤源不可用时，系统会保留可用缓存或显示默认头像，而不是暴露上游错误详情。

## 解除绑定

解除绑定会影响以该身份为依据的展示和集成。界面会在确认前列出实际后果；存在主账号、进行中操作或其他完整性约束时，系统会拒绝解除并给出当前状态。

资源包和商城贴图由管理员/运维配置，参见 [Minecraft 资源包与商城贴图](/docs/operations/minecraft-resource-packs/)。
